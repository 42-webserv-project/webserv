<!-- Looks good! I added two tiny details but overall I agree, it makes sense and it seems complete! Then again, I don't have a full overview yet -->

1. What does an HTTP response consist of, concretely? What does it output?

	An HTTP response consists of 4 parts:
	1. Status line (HTTP version, status code) -> the answer (success/fail)
	2. Headers (key-value pairs, one per line to let the client know how to handle the body) -> the instructions (how to)
	3. Blank line (separating headers from body)
	4. Body (actual content) -> the content (what)

	It is ouputted as raw bytes over the socket (ie. the pipe associated by the server to that client).

2. What does "response generation logic" mean concretely?

	The person responsible for the response generation, hereafter called "the response handler" would: 
		1. read files, run CGI, enforce config 
		2. then generate the response object 
		3. serialize it to bytes (build a complete response string by concatenating the response object)
		4. send it over the socket.

2. If I were to receive the most simple HTTP response (a GET request with an existing static HTML file), how would I implement the output generation for that response?

	1. Check if the file exists at the requested path
	2. Read the file into memory as a string
	3. Determine the Content-Type (in this case, text/html)
	4. Get the file size to set Content-Length
	5. Build the response string with the 4 parts described above.
	6. Send it over the socket. 
<!-- I would maybe swap step 2 and 3, since the content type determines whether we use CGI or not. Just for future implementation -->

	Example:
		// Read file
		std::string body = read_file("file.html");

		// Build response
		std::string response = "HTTP/1.1 200 OK\r\n";
		response += "Content-Type: text/html\r\n";
		response += "Content-Length: " + std::to_string(body.length()) + "\r\n";
		response += "\r\n";
		response += body;

		// Send to client
		send(client_socket_fd, response.c_str(), response.length(), 0);
<!-- Not too sure, but I think it would be better to have the response be an object instead of a string and then have a function that concatenates all the (relevant) objects to a string. So; response._contentType = "text/html\r\n" etc. -->
3. I need to mock this basic config (a HTTP request object for a GET request with an existing static HTML file), help me build it.

class HttpResponseState {
	private:
		const std::string httpVersion = "HTTP/1.1";
		enum statusCode;
		std::map<std::string, std::string> headers; // TODO: change datastructure, as it would lose duplicates
		std::vector<std::byte> body;

	public:
		HttpResponseState();
		HttpResponseState(const HttpResponseState &src);
		HttpResponseState& operator=(const HttpResponseState &src);
		~HttpResponseState;
}

TO DO:
- extend to other methods (POST etc)
- then handle more complex request types

- implement github actions


Make file validation deterministic first in src_http_response.cpp.
Right now existence plus stream-open is not enough. You want clear rules for missing path, non-regular file (directory), unreadable file, and readable regular file. \
This removes platform-dependent behavior and stabilizes both runtime and tests.

Fix response serialization correctness in http_response_state.cpp.
This is the most critical bug area: headers are not actually appended in the header loop, status text is hardcoded, and the response format should always be status line + headers + blank line + body. Until this is fixed, GET may appear to work but wire output is incomplete.

Remove workflow inconsistencies in src_http_response.cpp.
You currently set headers twice in the success path and do not build a proper serialized error response path. A complete GET workflow should produce a valid response for both success and failure, not just return an error int.

Implement real Content-Type mapping and fallback in src_http_response.cpp.
Extension-only output is a temporary step. For complete GET behavior, map common extensions to MIME types and use a safe default for unknown or missing extension.

Define status policy clearly and apply it consistently across method and file checks in http_response_state.hpp and src_http_response.cpp.
Decide exact status for unsupported method and keep reason phrase aligned with status code in serialization.

Tighten tests after each fix in http_response_assert.cpp.
As each behavior becomes correct, turn on the strict toggles you already prepared. This lets your tests evolve with the implementation instead of rewriting test structure later.

If you want, the next concrete move can be just one focused milestone: make deterministic file classification plus fully correct serialization, then immediately enable strict Content-Length checks in your assert file. That gives the biggest jump in GET quality with minimal scope creep.