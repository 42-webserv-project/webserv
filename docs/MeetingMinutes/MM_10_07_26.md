# Meeting Minutes — July 10, 2026
*Present: Johanna, Zacharia*

## Done Last Week
- Lexer for ConfigurationParser: Completed the base tokenization required to read the configuration file.
- Response Generation Test Suite: Finalized the test suite to validate current response building logic against mock inputs..

## TODO

### Joint Tasks (Johanna & Zacharia)

**1. Audit and Map Configuration Parameters from the Project Subject**

- **Task:** Read and extract all explicit configuration file requirements mandated by the webserv subject documentation.
- **Why it Matters:** The configuration file dictates vital execution behaviors, including interface-port pairs , default error pages , maximum client body sizes (client_max_body_size) , and route-specific constraints (such as accepted HTTP methods, redirections, directory listings, and CGI execution paths). We must map these variables now so that our downstream data structures can accommodate them natively.

**2. Design the Config-to-Response Interface**

- **Task:** Define the data exchange format and architectural boundaries between the Configuration Parser and the Response Generator.
- **Why it Matters:** The Response Generator cannot independently decide how to handle a request URI. It relies strictly on the configuration state to enforce route-level policies—such as checking if a POST or DELETE request is authorized on a given path , executing an HTTP redirection , triggering directory listing , or injecting custom error pages. Locking down this interface prevents architectural drift. .

### Johanna

**1. Refactor Response Generator Branch**

• **Task:** Clean up and standardize the Response Generator codebase to strictly match project coding conventions and mandatory guidelines.
• **Why it Matters:** Ensures we are maximizing native C++ standard features and industrial project conventions , properly abstracting HTTP response status codes, and avoiding code style discrepancies before merging this core engine into the main development branch.

### Zacharia

**1. Architect Abstract Syntax Tree (AST) & State Machine Workflow for ConfigParser**

- **Task:** Design the AST data structures and implement the state machine logic to transition token sequences into structural configuration data.
- **Why it Matters:** A robust parsing architecture is necessary to accurately evaluate contexts like nested server or location blocks (similar to NGINX). Utilizing a formal state machine guarantees syntactical resilience, preventing the server from crashing or misconfiguring routes if an invalid file is passed as a command-line argument.

**2. Integrate Parser and Response Engines into Alpha Server Event Loop**
*(Since Johanna is quite busy with an internship, Zach will try to take care of this as he has more direct communication with Tetiana who will be creating the Server Event Loop)*

- **Task:** Wire the current request parser and response generator directly into the network event loop (poll(), select(), kqueue(), or epoll()).
- **Why it Matters:** This is our highest-stakes milestone: achieving a full, single-request lifecycle (Read -> Parse -> Process -> Build Response -> Write). Because the subject strictly demands a non-blocking, single-multiplexed loop where no read or write happens outside the event notifier , testing this end-to-end flow early allows us to spot hanging connections  or improper socket state changes before they compound.

**3. Support: Response Generator**

- **Task:** Assist Johanna in optimizing and validating the Response Generator logic. implement other methods (delete, post, cgi etc).
- **Why it Matters:** The Response Generator must efficiently handle complex operations like un-chunking data transfers, routing scripts to the CGI subsystem via environment variables , and serving static assets securely. Double-teaming this module guarantees a highly resilient component capable of handling stress-testing without unexpected termination.
