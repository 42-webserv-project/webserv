## 03-07

Work Flow Config Parser:

Read file
Remove comments
Remove Optional WhiteSpace (Anything more than a single space (and tab?))

Three states:
- Global: Not really relevant for us I believe.
|
|---- Server: Contains information for a server block, think of listen/port, ip etc.
    |
    |
    |--- Location: Contains information about a specific location for the serverblock it is in
             ./adminOnly, acceptedMethod = None

Each state is a level higher than the previous. We have to end with an ending bracket at the global state.
Otherwise the logic of the config file is inaccurate and we must use a default config file.

---

G L O B A L
(none lol)

S E R V E R
- Setup all the interface:port pairs on which your server will listen to
        What does this mean?
- Set up default error pages -> What keyword should this be?
- Set the maximum allowed size for client request bodies -> Keyword?

L O C A T I O N
- Specify rules/configs on a URL/route, for a website
    - List of accepted methods on the route
    - HTTP Redirection
    - Directory where the requested file should be located
    - Enabling/disabling directory listing (what even is that?)
    - Default file to serve when the requested resource is a directory
    - Uploading files from the clients to the server is authorized and storage location is provided
