## 17-07

So I made my own test file which ideally I had done in the test folder. 
However for debugging it would be nice to have information printed on screen
so I can see what is going on. Currently test function does not do anything as such.

However, perhaps this is what we can use the logger for? 
That would be cool.

But for now, I htink I need to wait for my teammates to see whether that is how we use the logger.
In the meantime, the clearest bug I can see are these:

- Comments not taken out. (this is my own doing in the test file)

- Ip's consist of digits and dots `.`, I separate them currently which I don't think is correct.

It also looks like I will need to make a lot of rules for parsing!

By rules I mean things like;

- Oh I found the `server` keyword. That means the next token must be a `symbol`. After that I can accept
the keywords `location` or ... FOUND OUT WHAT KEYWORDS CAN FOLLOW AFTER EACH OTHER. AKA, make a flowchart....

Yeah you're right I have to make a flowchart...

The first one is obvious;

The first token must always be `Keyword` of value Server.
This has to be followed by a `symbol` of value `{`

Now we can have the following keywords;
- listen
- server_name
- host
- root
- client_max_body_size
- index
- error_page
- location
- end of block `}`

If we have the `location` keyword we get:
must be followed by an identifier (the path for our location)
Must be followed by a `{` symbol.
- method
- return
- upload_store
- autoindex
- index
- cgi path
- `}` (end of location block)
